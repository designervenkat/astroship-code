---
const { title, description } = Astro.props;
console.log(`title is = ${title}`)
console.log(`description is = ${description}`)
---

<h1 class="text-center mt-20 text-xl lg:text-4xl font-bold">{title}</h1>
<p class="text-center my-4 text-slate-500">{description}</p>